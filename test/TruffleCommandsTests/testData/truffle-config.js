const HDWalletProvider = require('truffle-hdwallet-provider');
const path = require('path');
const fs = require('fs');
module.exports = {
  networks: {},
  mocha: {},
  compilers: {
    solc: {},
  },
};
